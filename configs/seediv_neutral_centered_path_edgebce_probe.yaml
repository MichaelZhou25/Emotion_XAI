extends: seediv_neutral_centered_path_eagle_probe.yaml

loss:
  lambda_path: 0.0
  lambda_edge: 0.05
  edge_loss_from_logits: true
  edge_positive_weight: 1.0

logging:
  save_dir: results/seed_iv/neutral_centered_path_supervised/edgebce_probe
