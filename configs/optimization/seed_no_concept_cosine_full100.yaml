extends: ../seed_hemi_mv_eagle_hierarchy_hpcl_v5_without_concept_full100.yaml

model:
  branch_weights:
    concept: 0.0

train:
  lr_scheduler: cosine
  lr_warmup_epochs: 5
  min_lr: 0.00001

loss:
  lambda_concept: 0.0

logging:
  save_dir: results/seed/hpcl/optimization/no_concept_cosine_full100
