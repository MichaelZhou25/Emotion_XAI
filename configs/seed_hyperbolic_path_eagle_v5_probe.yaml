extends: seed_hyperbolic_path_eagle_v5_full100.yaml

protocol:
  target_subjects: [0, 3, 7, 10, 14]
  fold_seed_mode: target

train:
  epochs: 40

logging:
  save_dir: results/seed/hyperbolic_path/v5_probe
