extends: seediv_edge_relation_balance_s2_full100.yaml

protocol:
  target_subjects: [0, 3, 4, 6, 11, 14]
  fold_seed_mode: target

train:
  epochs: 40
  lr_decay_epochs: 100
  target_adaptation:
    enabled: true
    target_domain_weight: 1.0

logging:
  save_dir: results/tuning/seediv_target_domain_uda_s2_probe
