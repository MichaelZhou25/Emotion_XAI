extends: ../seediv_hyperbolic_node_edge_v8_full100.yaml

model:
  use_adaptive_endpoint_fusion: true
  detach_endpoint_gate_features: true
  edge_endpoint_gate_hidden: 16
  edge_endpoint_gate_max_weight: 1.0

train:
  lr_decay_epochs: 40

logging:
  save_dir: results/tuning/seediv_gate_full15/gate_only/s1
