extends: seediv_hyperbolic_node_edge_v8_full100.yaml

train:
  epochs: 40
  lr_decay_epochs: 100
  target_adaptation:
    enabled: true
    target_domain_weight: 1.0
    schedule: linear
    warmup_epochs: 10
    ramp_epochs: 10

logging:
  save_dir: results/seed_iv/target_domain_uda/session1_phase40
