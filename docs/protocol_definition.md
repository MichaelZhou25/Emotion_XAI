# Protocol Definition

## strict_dg_loso

The primary leakage-free protocol. The target subject is not used for training, validation, checkpoint selection, early stopping, or hyperparameter selection.

## legacy_loso

A paper-aligned protocol matching prior open-source implementations that select checkpoints using the held-out target subject. This is reported only for historical comparison.
